<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Sequences for -->
        <record id="seq_muon_tra" model="ir.sequence">
            <field name="name">Mã phiếu mượn sách</field>
            <field name="code">muontrasach.code</field>
            <field name="prefix">MT</field>
            <field name="padding">5</field>
            <field name="company_id" eval="True"/>
        </record>

        <!-- Sequences for  -->
        <record id="seq_doc_gia" model="ir.sequence">
            <field name="name">Mã độc giả</field>
            <field name="code">docgia.code</field>
            <field name="prefix">DG</field>
            <field name="padding">5</field>
            <field name="company_id" eval="True"/>
        </record>

        <!-- Sequences for -->
        <record id="seq_ma_sach" model="ir.sequence">
            <field name="name">Mã sách</field>
            <field name="code">masach.code</field>
            <field name="prefix">MS</field>
            <field name="padding">5</field>
            <field name="company_id" eval="True"/>
        </record>
    </data>
</odoo>